export enum DashboardMenus {
  ENVIRONMENTS = 'environments',
  AGENTS = 'agents',
  REQUESTED_CERTS = 'requested-certs',
  SIGNED_CERTS = 'signed-certs',
  UNSIGNED_CERTS = 'unsigned-certs',
  TASKS = 'tasks',
  MODULES = 'modules',
  CLASSES = 'classes'
}
